<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ t "New thread" . }}</title>
</head>
<body>
    <div class="container-fluid">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/forum">Forum</a></li>
                <li class="breadcrumb-item active">{{ t "New thread" . }}</li>
            </ol>
        </nav>


        <div class="card mb-3">
            <div class="card-header">
                {{ t "New thread" . }}
            </div>
            <div class="card-body">
                <form method="post" novalidate>
                    <input type="hidden" name="csrf" value="{{ .CSRF }}" />
                    <div class="form-group">
                        <label for="thread_name">{{ t "Thread name" . }}</label>
                        <input name="thread_name" id="thread_name" placeholder="Thread name" value="{{ .Data.ThreadName }}" class="form-control{{ if .Data.ErrorThreadName }} is-invalid{{ end }}" type="text" {{ if .Data.ErrorThreadName }} autofocus{{ end }} required minlength="5" aria-describedby="thread-name-error" />
                        {{ if .Data.ErrorThreadName }}<div class="invalid-feedback" id="thread-name-error">{{ .Data.ErrorThreadName }}</div>{{ end }}
                    </div>
                    <div class="form-group">
                        <label for="message">{{ t "Message" . }}</label>
                        <textarea name="message" id="message" placeholder="Message" rows="10" class="form-control{{ if .Data.ErrorMessage }} is-invalid{{ end }}"
